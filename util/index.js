const deliverToken = require('./deliverToken');
const response = require('./objectResponse');
const verifyEmailAndPassword = require('./verifications');

module.exports = {
  deliverToken,
  response,
  verifyEmailAndPassword,
};
